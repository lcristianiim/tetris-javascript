function keyPress () {
    let up = 38;
    let down = 40;
    let left = 37;
    let right = 39;


    var key = event.keyCode || event.which;
    // up key
    if (key == 38) {
    }

    // down key
    if (key == 40) {
    }

    // left key
    if (key == 37) {
    }

    // right key
    if (key == 39) {
        console.log('Maximum right side');
    }
}

var me = 'hello';
